# 🛡️ Правила разработки Kanban AI Agent

## 🎯 Основной принцип: НЕ ЛОМАТЬ СУЩЕСТВУЮЩИЙ КОД

### ✅ ЧТО МОЖНО ДЕЛАТЬ:

#### 📁 Создание новых файлов и папок

- ✅ Создавать новые модули в `src/`
- ✅ Добавлять новые сервисы
- ✅ Создавать новые контроллеры
- ✅ Добавлять новые интерфейсы в `src/types/`
- ✅ Создавать новые DTO в `src/dto/`
- ✅ Добавлять новую документацию в `docs/`

#### 🔧 Расширение существующих файлов

- ✅ Добавлять новые enum значения в конец существующих enum'ов
- ✅ Добавлять новые интерфейсы в файлы типов
- ✅ Добавлять новые методы в существующие сервисы (в конец класса)
- ✅ Добавлять новые импорты в начало файлов
- ✅ Добавлять новые зависимости в конструкторы (в конец списка)

#### 🔌 Интеграция через Dependency Injection

- ✅ Инжектировать новые сервисы в существующие через конструктор
- ✅ Создавать новые модули и импортировать их в AppModule
- ✅ Использовать существующие сервисы в новых модулях

#### 📦 Управление пакетами

- ✅ Устанавливать новые зависимости через `yarn add`
- ✅ Обновлять package.json с новыми зависимостями

### ❌ ЧТО НЕЛЬЗЯ ДЕЛАТЬ:

#### 🚫 Изменение существующей логики

- ❌ Изменять существующие методы в сервисах
- ❌ Модифицировать логику в webhook.service.ts
- ❌ Менять существующие интерфейсы и типы
- ❌ Изменять существующие enum значения
- ❌ Переписывать существующие файлы

#### 🚫 Нарушение архитектуры

- ❌ Удалять существующие файлы
- ❌ Переименовывать существующие файлы
- ❌ Перемещать существующие файлы
- ❌ Изменять структуру существующих модулей

#### 🚫 Конфликты зависимостей

- ❌ Удалять существующие зависимости
- ❌ Изменять версии существующих пакетов без необходимости
- ❌ Создавать циклические зависимости между модулями

### 🎯 СТРАТЕГИИ РАСШИРЕНИЯ:

#### 1. 📋 Для новой функциональности workflow:

```
src/workflow/
├── workflow.module.ts          // Новый модуль-оркестратор
├── workflow.service.ts         // Управление переходами статусов
├── transitions.service.ts      // Логика переходов между статусами
└── interfaces/
    └── workflow.interface.ts   // Новые типы для workflow
```

#### 2. 🔄 Для расширения статусов:

```typescript
// В src/types/enums.ts - ТОЛЬКО ДОБАВЛЯТЬ в конец
export enum TaskStatus {
  NEW = 'new', // Существующий
  QUESTIONS = 'questions', // Существующий
  IN_PROGRESS = 'in_progress', // Существующий
  REVIEW = 'review', // Существующий
  DONE = 'done', // Существующий
  // ✅ Новые статусы добавляем здесь:
  BACKLOG = 'backlog', // Новый
}
```

#### 3. 🎮 Для новых событий webhook:

```
src/events/
├── events.module.ts           // Новый модуль событий
├── event-handler.service.ts   // Обработчик новых событий
└── types/
    └── event.interface.ts     // Типы новых событий
```

#### 4. 🔗 Интеграция через композицию:

```typescript
// В существующий сервис добавляем ТОЛЬКО инжекцию
constructor(
  private readonly aiAnalysisService: AIAnalysisService,  // Существующий
  private readonly kanbanService: KanbanService,          // Существующий
  private readonly taskExecutorService: TaskExecutorService, // Существующий
  // ✅ Новые сервисы добавляем в конец:
  private readonly workflowService: WorkflowService,      // Новый
) {}

// И вызываем новую логику через новые методы:
async processNewIssue(payload: JiraWebhookDto) {
  // Существующая логика остается

  // ✅ Добавляем новую логику в конце:
  await this.workflowService.handleStatusTransition(payload);
}
```

### 🔍 ПРОВЕРКА ПЕРЕД ИЗМЕНЕНИЯМИ:

#### Вопросы перед любым изменением:

1. ❓ Это новый файл или изменение существующего?
2. ❓ Будет ли это влиять на существующую функциональность?
3. ❓ Можно ли это реализовать через новый модуль?
4. ❓ Нужно ли изменять существующие интерфейсы?

### 📋 ПЛАН БЕЗОПАСНОГО РАСШИРЕНИЯ WORKFLOW:

#### Этап 1: Новые типы и интерфейсы

- ✅ Создать `src/workflow/interfaces/` с новыми типами
- ✅ Добавить новые enum значения в `src/types/enums.ts`

#### Этап 2: Новые сервисы

- ✅ Создать `WorkflowService` для управления переходами
- ✅ Создать `TransitionService` для логики переходов
- ✅ Создать `StatusManagerService` для управления статусами

#### Этап 3: Интеграция

- ✅ Инжектировать новые сервисы в существующие
- ✅ Добавить вызовы новых методов в конец существующих методов
- ✅ Создать новые endpoints для тестирования (опционально)

#### Этап 4: Тестирование

- ✅ Проверить, что существующая функциональность не сломалась
- ✅ Протестировать новую функциональность отдельно
- ✅ Интеграционное тестирование

### 🎉 РЕЗУЛЬТАТ:

Расширенная функциональность без риска поломки существующего кода!
